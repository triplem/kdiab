services:
  keycloak:
    image: quay.io/keycloak/keycloak:23.0
    command: start-dev --import-realm
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
    volumes:
      - ./config/keycloak-realm.json:/opt/keycloak/data/import/realm.json
      - ./config/keycloak-theme:/opt/keycloak/themes/kdiab:ro
    ports:
      - "8085:8080"
    restart: unless-stopped

  analyze-backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      MEASURES_URL: ${MEASURES_URL:-http://host.docker.internal:8080}
      PROFILES_URL: ${PROFILES_URL:-http://host.docker.internal:8082}
      TREATMENTS_URL: ${TREATMENTS_URL:-http://host.docker.internal:8083}
      JWT_DOMAIN: ${JWT_DOMAIN:-http://localhost:8085/realms/kdiab-analyze}
      JWKS_URL: ${JWKS_URL:-http://keycloak:8080/realms/kdiab-analyze/protocol/openid-connect/certs}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-analyze}
      JWT_REALM: ${JWT_REALM:-kdiab-analyze}
      JSON_PRETTY_PRINT: "true"
    ports:
      - "8084:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      keycloak:
        condition: service_started
    restart: unless-stopped
