services:
  postgres:
    image: postgres:15.18@sha256:eb37f58646a901dc7727cf448cae36daaefaba79de33b5058dab79aa4c04aefb
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-kdiab-profiles}
      # Per-service role password — passed to 01-init-role.sh at first startup.
      PROFILES_DB_PASSWORD: ${PROFILES_DB_PASSWORD:-kdiab_profiles_devpassword}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./config/postgres/01-init-role.sh:/docker-entrypoint-initdb.d/01-init-role.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-kdiab-profiles}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.6.1@sha256:dea26401d06341095cc4ea9d66896200b55de5ca1daa1d2fcbe58493afa6e0ad
    command: start-dev --import-realm
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
    volumes:
      - ./config/keycloak-realm.json:/opt/keycloak/data/import/realm.json
      - ./config/keycloak-theme:/opt/keycloak/themes/kdiab
    ports:
      - "8081:8080"
    restart: unless-stopped

  liquibase:
    build:
      context: .
      dockerfile: Dockerfile.liquibase
    command: >
      --url=jdbc:postgresql://postgres:5432/${POSTGRES_DB:-kdiab-profiles}
      --username=${POSTGRES_USER:-postgres}
      --password=${POSTGRES_PASSWORD:-password}
      --searchPath=/liquibase
      --changeLogFile=db/changelog/db.changelog-master.yaml
      update
    depends_on:
      postgres:
        condition: service_healthy

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      JDBC_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-kdiab-profiles}
      DB_USER: kdiab_profiles
      DB_PASSWORD: ${PROFILES_DB_PASSWORD:-kdiab_profiles_devpassword}
      JWT_DOMAIN: http://localhost:8081/realms/kdiab-profiles
      JWKS_URL: http://keycloak:8080/realms/kdiab-profiles/protocol/openid-connect/certs
      JWT_AUDIENCE: profile
      JWT_REALM: kdiab-profiles
      CORS_ALLOWED_ORIGINS: http://localhost:3001
      LOG_LEVEL: DEBUG
      JSON_PRETTY_PRINT: "true"
    ports:
      - "8082:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_started
      liquibase:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  postgres_data:
