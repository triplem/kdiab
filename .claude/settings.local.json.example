{
  "_comment": "Copy to settings.local.json (gitignored) for personal overrides. These take precedence over settings.json.",
  "env": {
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "85",
    "_comment_AWS_PROFILE": "Only needed for the optional AWS MCP servers (pricing/IaC) — not for models. This project uses Anthropic's default models, not Bedrock. Set your AWS SSO/credential profile here (kept out of shared settings.json so it doesn't leak) if you use those servers.",
    "_comment_AWS_AIDLC_DEFAULT_SCOPE": "Pre-set default scope for /aidlc workflows. Valid: enterprise, feature, mvp, poc, bugfix, refactor, infra, security-patch, workshop. Applies at workflow init only. See docs/guide/12-customization.md § Per-Project Default Scope.",
    "AWS_AIDLC_DEFAULT_SCOPE": "workshop"
  },
  "model": "opus[1m]",
  "effortLevel": "xhigh"
}
