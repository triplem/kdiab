<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-08-20T06:00:00Z — Implemented + VERIFIED the core slice (T2+T3+T1) on branch feature/1606-jackson-free-jwt. kdiab-common compiles against real Nimbus 10.0.1 (the design's API usage was correct first-try except the Ktor provider `name` String? shadow). Full Nimbus Security.kt rewrite; 11/11 parity tests PASS incl. the reviewer must-fix (non-array roles → 401 not 500); full :kdiab-common:test green; Security.kt Detekt-clean.
- 2026-08-20T06:00:30Z — Scoped the slice to NOT touch the shared ktor-server bundle (only kdiab-common's own dep), so the other 8 services stay green until T4. Shared TestTokenMinter across services (Q1=C) needs composite-build test-fixtures wiring — deferred to T4; for T1 I put TestTokenMinter in kdiab-common src/test.
- 2026-08-20T19:30:00Z — RESUME (session 2): finished T2-remainder, T4, T5, T6, T7. Q1 RESOLVED to **A (per-service Nimbus minter), NOT C**: this slice's plan.md + the session-1 diary leaned toward a shared kdiab-common test-fixture (option C), but the AUTHORITATIVE upstream artifact — application-design/decisions.md ADR-023a — decided **Q1=A** and explicitly REJECTED the shared fixture ("more refactoring than #1606 warrants"). Per the project traceability rule (cite the synthesized authoritative artifact, not a downstream working note), I followed ADR-023a. A is also the lower-risk path (no composite-build testFixtures wiring — the exact thing the session-1 diary flagged as hard). Nimbus test dep added once in `kdiab.ktor-service` convention plugin (`testImplementation(nimbus)`), inherited by test/integrationTest/e2eTest for all 8 services.
- 2026-08-20T19:45:00Z — VERIFIED all 25 service test files migrated (auth0 → Nimbus SignedJWT+MACSigner) + kdiab-common TestTokenMinter (already Nimbus in core slice). 19 regular files done via a scoped transform script (scratchpad/migrate_jwt.py) + per-module compile safety-net; 4 profiles files (InsulinApiTest/ProfileApiTest/InsulinE2ETest/ProfileE2ETest) done by hand (they use `"secret"` as the legit signing+config secret). All 8 services + kdiab-common test sources compile; kdiab-common parity matrix, measures full suite (unit+integration+e2e), and profiles test+integrationTest (incl. SecurityConfigTest) all GREEN.

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-08-20T06:01:00Z — NEW FINDING (verified at compile/test time): Nimbus HS256 REQUIRES a >=32-byte secret; java-jwt did not. The existing test secret "test-secret-for-unit-tests-only" (31 bytes) FAILS with Nimbus → T4 must lengthen every service's test secret to >=32 bytes (and any jwt.test=true prod/staging JWT_SECRET). Documented in code-summary.md. Also: kdiab-common detektMain is red on PRE-EXISTING UnreachableCode false-positives in untouched RateLimit/AuditRoutes/Tracing (not baselined; stale baseline after Kotlin 2.3.20) — out of #1606 scope, flagged for separate confirmation vs main CI.

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-08-20T20:10:00Z — **T6 KEY FINDING (deviation from epic premise):** removing `ktor-server-auth-jwt` eliminated `java-jwt`/`jwks-rsa` from every runtimeClasspath (✓ AC-10) but jackson was STILL present at 2.21.4 — the jackson force-pin `constraints{}` block ITSELF was materialising it (versioned `constraints{ implementation(…) }` kept the modules resolved with no real consumer). The epic's premise ("java-jwt is the last jackson consumer") was thus incomplete: after #1607 (swagger path) + #1606 (jwt path), the CONSTRAINT was the last thing. Removing the two jackson constraint lines cleared jackson from ALL 9 runtimeClasspaths with NO downgrade to the vulnerable 2.21.3 (jackson simply vanished). handlebars pin kept (4.5.2, CVE-2026-55760). This matches ADR-023c's gated action, but the mechanism differed from what was expected — documented in ADR-023 Consequences.
- 2026-08-20T20:15:00Z — Secret-lengthening surface was BROADER than the 25 auth0 files. Nimbus MACSigner + the new `HMAC_MIN_SECRET_BYTES=32` require reject <32-byte HS256 secrets that java-jwt tolerated. Also lengthened 6 non-auth0 app-startup config tests (per-service ApplicationTest.kt + users InternalRoutesTest.kt) and profiles SecurityConfigTest happy-path (`"test-secret-value"` 17B → ok), which set `jwt.test=true` + a short `jwt.secret` and start the app. Left the SecurityConfigTest NEGATIVE test (omitted secret) untouched — it correctly still asserts startup failure.
- 2026-08-20T20:20:00Z — Nimbus `JWTClaimsSet.Builder.audience` has no vararg overload (java-jwt's `withAudience(vararg)` does). The 4 analyze multi-audience tokens were fixed to `.audience(listOf(...))` after the mechanical transform produced an invalid vararg call.
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
- 2026-08-20T20:25:00Z — PRE-EXISTING flaky composite-build race (OUT of #1606 scope, flag for build-and-test/CI): nightscout's `registerUpstreamSpec` generation (`generateProfilesModels`) can non-deterministically resolve the WRONG upstream `apiSpec` (once generated MEASURES models into `upstream-profiles`), and the wrong output gets cached in the Gradle build cache (which `clean` does NOT clear), so it deterministically re-fails until `--no-build-cache --rerun-tasks`. I hit this via my own stash/pop diagnostics; it is NOT caused by the #1606 code (nightscout builds green with a clean cache). Matches the existing memory note about the flaky openApiGenerate-vs-compileKotlin parallelism race. Recommend a follow-up to make the `apiSpec` variant selection unambiguous (per-service Category attribute value), independent of #1606.
- 2026-08-20T20:26:00Z — Pre-existing kdiab-common `detektMain` reds on UnreachableCode false-positives in untouched RateLimit/AuditRoutes/Tracing (stale baseline post Kotlin 2.3.20) remain out of #1606 scope — flagged for build-and-test to confirm vs main CI. My changed main files (Security.kt core slice, ProfileRoutes.kt dead-import removal) are detekt-clean.
- 2026-08-20T20:27:00Z — Removing `ktor-server-auth-jwt` from the bundle required deleting a DANGLING `import io.ktor.server.auth.jwt.*` from `ProfileRoutes.kt` (main) — it was unused (route handlers use `call.principal<UserPrincipal>()` from base `io.ktor.server.auth.*`). No other main file imported the jwt package.
