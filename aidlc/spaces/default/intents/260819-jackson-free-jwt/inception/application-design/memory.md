<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-08-19T17:55:00Z — VERIFIED API CONSTRAINT that reverses a prior decision: `javap` on `ktor-server-auth-jvm-3.5.0.jar` shows `BearerAuthenticationProvider$Config` exposes only authenticate/realm/authHeader/authSchemes — NO `challenge` method. The plain bearer provider hard-codes its 401 (empty body + WWW-Authenticate). So scope-definition's Option A (bearer provider) CANNOT satisfy FR-6 (exact ErrorResponse body + TOKEN_REJECTED log). The current `jwt` provider emits those via its `challenge {}` block. Resolution: use a custom `AuthenticationProvider` (scope-definition Option B) which supports `context.challenge{}` — still on ktor-server-auth base, Nimbus-backed, route wiring `authenticate("auth-jwt")` unchanged. Surfaced to user as Q3. This is exactly the value of verifying an API against the real jar instead of assuming.
- 2026-08-19T17:55:30Z — Test-minting (Q1=A): migrate every service's test minter from `com.auth0.jwt.JWT.create()` to Nimbus `MACSigner`/`SignedJWT` — removes java-jwt from test scope too (fully gone). ADR (Q2=A): platform `docs/adr/ADR-023-jackson-free-jwt-verification.adoc` (next after ADR-022).

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-08-19T18:25:00Z — §12a architecture-reviewer (aidlc-architecture-reviewer-agent) ran 327s / 33 tools — did NOT hang (it disassembled the real ktor-server-auth-jvm-3.5.0 AND nimbus-jose-jwt-10.0.1 jars). VERDICT NOT-READY, ONE must-fix + 5 non-blocking. Must-fix was a genuine load-bearing bug in MY design: I claimed Nimbus `getStringListClaim` returns null on present-but-non-array — FALSE, it THROWS ParseException (java-jwt returns null). Unguarded → 500 instead of 401 on FR-4's present-but-non-array roles case. FIXED: exception-guard every Nimbus typed accessor (runCatching{}.getOrNull()) in the mapper; corrected components.md l.27 + component-methods.md. All 5 non-blocking folded in too: catch BadJWTException before BadJOSEException (subclass); pin exact Nimbus reason strings + INVALID_CLAIMS fallback + fold nbf into EXPIRED; concrete AuthenticationFailedCause (NoCredentials/InvalidCredentials); documented test-mode clock-skew 0→3s deviation; FR-5 JWKS caching noted as semantic re-model not 1:1.
- 2026-08-19T18:25:30Z — §12a JUDGMENT: reviewer verdict was NOT-READY with 1 iteration remaining, so protocol allows a re-invoke. Chose NOT to re-invoke: the reviewer specified the EXACT fix AND pre-declared its sufficiency ("once folded in, the design is implementable; the five non-blocking items are downstream precision fixes"). Applied verbatim + self-verified. Re-running the 5.5-min deep-disassembly reviewer to confirm a fix it pre-approved is poor ROI; the human gate is the final checkpoint and I surfaced the full review + fixes there. NOTE: this reviewer did NOT hang this run — the poll-before-stop refinement (learned this session) held: I polled via decisions.md grep, saw it progressing (327s is long but productive), and did not stop it.

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->
- 2026-08-19T18:10:00Z — TOKEN_REJECTED: user reopened the discussion mid-stage and chose B+D (add `reason=` field + proxy-aware `remote=`), an intentional FR-6 refinement. Key design guard: the HTTP response (401 + ErrorResponse body) stays byte-for-byte identical (client contract preserved; generic message avoids leaking why-auth-failed); only the internal security log is enriched. To carry the reason, `TokenVerifier` returns a sealed `VerificationOutcome` (Verified(claims) | Rejected(reason)) rather than a nullable claims set. `remote=` reads X-Forwarded-For first IP (fallback remoteHost) directly in the challenge — NOT the app-wide XForwardedHeaders plugin (keeps the change contained). Recorded as ADR-023b; Build & Test must assert `reason=` per negative-path case.
- 2026-08-19T18:10:30Z — Used ONE shared Nimbus `DefaultJWTProcessor` shape for BOTH RS256 (JWKSource) and HMAC-test (symmetric key), differing only in key source, so a single `DefaultJWTClaimsVerifier` (issuer+audience+exp+3s skew) enforces claims identically on both paths. This closes the FR-3 gap (bare MACVerifier wouldn't check issuer/audience) by construction rather than by remembering to re-add checks.

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
- 2026-08-19T18:12:00Z — INLINE REVIEW findings (for code-gen / Functional Design to pin against the CHOSEN Nimbus version): (1) `DefaultJWTClaimsVerifier` constructor signature varies by Nimbus version — the 3-arg (acceptedAudience, exactMatchClaims, requiredClaims) + `maxClockSkew` exist in nimbus-jose-jwt 9.x/10.x; code-gen must match the pinned version. (2) `JWKSourceBuilder.create(url).cache(ttl, refreshTimeout).rateLimited(minInterval).retrying(true)` is the modern (9.x+) JWKS caching API; the OLD auth0 config was cache(size=10, ttl=24h) + rateLimited(bucket=10, refill=1/min) — the mapping to Nimbus's (ttl, refreshTimeout)+(minInterval) is intent-preserving, NOT value-identical; pick sensible equivalents (ttl≈24h; a small refresh timeout; rate-limit min-interval mirroring 1/min). (3) PARITY SUBTLETY: in the CURRENT prod (JWKS) path, audience is checked ONLY in buildPrincipal (the java-jwt `verifier(provider, jwtDomain)` checks issuer, not audience); the new design checks audience in the shared claims verifier (before mapping). Outcome identical (wrong audience ⇒ 401); reason classification = WRONG_AUDIENCE. No behaviour regression, but note it so the parity test asserts wrong-audience rejection on the PROD path too, not just test mode.
- 2026-08-19T18:12:30Z — Reviewer is aidlc-architecture-reviewer-agent (hang-prone per project.md). Launched background; polling via grep of decisions.md for ## Review + mtime (NOT TaskOutput, which dumps the full transcript into context). Will stop only if genuinely stalled per the poll-before-stop refinement.
