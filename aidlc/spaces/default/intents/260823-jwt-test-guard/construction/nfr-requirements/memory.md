<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
- 2026-08-23T00:00:00Z — security-patch has no units (units-generation/functional-design skipped). CORRECTED placement: nfr-requirements has `for_each: unit-of-work`, so aidlc-state.ts's approve-time existence check globs construction/<unit>/nfr-requirements/ for every subdir of construction/. The engine's memory_path is stage-level (construction/nfr-requirements/memory.md) but the 5 produced ARTIFACTS + questions must live under a UNIT dir. Used the intent slug as the single unit → construction/jwt-test-guard/nfr-requirements/ (matches the prior release-workflow-fix intent's split: memory at construction/<stage>/, artifacts at construction/<slug>/<stage>/). Same convention applies to the remaining for_each construction stages (code-generation).
- 2026-08-23T00:00:00Z — treated this NFR stage as security-led: the only substantive NFR is the security-requirement (the production guard). performance/scalability/reliability/tech-stack artifacts are produced but are near-N/A (startup-time config check, no new tech), documented briefly rather than padded.

## Deviations
- 2026-08-23T00:00:00Z — consumes lists functional-design + requirements-analysis artifacts that don't exist in security-patch scope (those stages are skipped). Grounded the NFRs instead in the finding (#1588 / FIND-SEC-001), the codekb technology-stack.md, and the actual Security.kt readJwtConfig code — the finding IS the requirement for a security-patch.

## Tradeoffs
- 2026-08-23T00:00:00Z — guard-signal design fork surfaced to the user (dedicated deny-by-default jwt.allowTestMode opt-in vs a general app.environment/KDIAB_ENV concept vs Ktor developmentMode). Recommended the dedicated opt-in: single-purpose, deny-by-default, minimal blast radius for a security-patch; avoids overloading a general flag and avoids relying on unverified Ktor test developmentMode behaviour.

## Open questions
- 2026-08-24T00:00:00Z — RESOLVED in fix loop: §12a reviewer (aidlc-architecture-reviewer-agent) completed cleanly this run (~90s, no hang, contrary to the project.md reviewer-hang correction — reinforces that the hang is agent+intent-specific, not universal). Verdict NOT-READY (iter 1): the artifacts' "existing tests stay green with no change" claim (SR-4/SR-5/RL-3/AC-2) was false — the deny-by-default guard breaks ~36 test-enablement sites unless each affirms allowTestMode, and SecurityConfigTest's own positive-path test (cited as precedent) would break. Applied all fixes: corrected SR-4/SR-5/RL-3/AC-2 wording, added SR-7 + TD-4 guard-precedence (opt-in guard before secret guard for deterministic messages), enumerated the two edit mechanisms (~34 builders + 2 resource confs). Re-invoking reviewer for iter 2 confirmation.
- 2026-08-24T00:00:00Z — §12a reviewer iter 2 verdict: READY. All 3 blocking findings resolved; reviewer independently re-verified the ~36-site count (34 .kt builders + 2 HOCON test confs) against the repo. Reviewer completed cleanly both iterations (~90s + ~21s), no hang — consistent with the project.md refinement that the aidlc-architecture-reviewer-agent hang is agent+intent-specific, not universal.
