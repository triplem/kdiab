# User Flow — Technology & Domain Review

**Status: NOT APPLICABLE**

No new user-facing flow is being designed. This is a technology + domain **review** (recommendations-only).

## The only relevant "flow" — how the review output is consumed

1. Maintainer reads the prioritized **backlog** (GitHub issues, grouped by theme).
2. Picks **quick wins** to action in the next burst.
3. Uses the **phased roadmap** to sequence larger items over time.

This is an async, single-actor consumption flow over documents — not an application interaction flow.
Existing kdiab UI flows are unchanged by this review (any UI change would be a separate future intent).
