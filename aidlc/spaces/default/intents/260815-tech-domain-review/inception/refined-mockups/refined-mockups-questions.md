# Refined Mockups — Questions

**Status: NOT APPLICABLE** — the standard UI clarifying questions have no meaningful answers for a
recommendations-only, no-UI intent.

## Why the standard questions do not apply

The stage's Step 3 questions all presuppose a UI being designed:

| Standard question | Applicability here |
|---|---|
| How is each story represented in the UI? | N/A — stories are assessment/deliverable stories, not screens (`stories.md`). |
| Interaction patterns (modals, wizards, inline edits)? | N/A — no application UI (`interaction-spec.md`). |
| Screen states (loading/empty/error/success)? | N/A — output is Markdown/GitHub artifacts. |
| Alignment with the design system? | N/A — no components designed (`design-system-mapping.md`). |
| Accessibility (WCAG level)? | N/A — no new UI surface (`accessibility-checklist.md`). |
| Responsive breakpoints? | N/A — documents, not screens. |
| For APIs: developer experience? | N/A — no new API surface; the review assesses the existing one. |

## The one settled decision

The only "presentation" decision — how the review output is delivered (GitHub issues + roadmap doc +
quick-wins under `docs/review/`) — was already answered in requirements (`requirements.md` FR-D.1–D.3,
RA-Q1 = C). No open questions remain for this stage.
