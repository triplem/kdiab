# Design-System Mapping — Technology & Domain Review

**Status: NOT APPLICABLE** — no components are designed, so there is nothing to map to a design system.

## No component mapping required

Because no UI is built (`mockups.md`), there are no components to map onto the existing `kdiab-ui`
design system (React 19 + the project's component library). The review does not add, modify, or
restyle any UI component.

## Note for future frontend recommendations

If the review's tech-debt/modernization findings (`stories.md` US-5/US-6) recommend `kdiab-ui` changes,
those recommendations are captured as backlog items — and any that become features would, in a future
intent, be mapped against the existing design system at that time. This review only *records* such
recommendations; it does not design them.
