# Frontend Components — Release Workflow Fix (#1617)

Consumes `../../../inception/requirements-analysis/requirements.md`. (`unit-of-work.md`,
`application-design/services.md` etc. are **N/A — skipped**.)

## Not applicable

#1617 is a CI/CD workflow-configuration change. There is **no user-facing surface** — no UI, no
`kdiab-ui` change, no component. `kdiab-ui` is explicitly out of scope (`business-rules.md` R-5); it is
already aligned. Nothing to design at the frontend layer.
