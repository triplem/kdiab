services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-kdiab-treatments}
      # Per-service role password — passed to 01-init-role.sh at first startup.
      TREATMENTS_DB_PASSWORD: ${TREATMENTS_DB_PASSWORD:-kdiab_treatments_devpassword}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./config/postgres/01-init-role.sh:/docker-entrypoint-initdb.d/01-init-role.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-kdiab-treatments}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:23.0
    command: start-dev --import-realm
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
    volumes:
      - ./config/keycloak-realm.json:/opt/keycloak/data/import/realm.json
      - ./config/keycloak-theme:/opt/keycloak/themes/kdiab
    ports:
      - "8081:8080"
    restart: unless-stopped

  liquibase:
    build:
      context: .
      dockerfile: Dockerfile.liquibase
    command: >
      --url=jdbc:postgresql://postgres:5432/${POSTGRES_DB:-kdiab-treatments}
      --username=${POSTGRES_USER:-postgres}
      --password=${POSTGRES_PASSWORD:-password}
      --searchPath=/liquibase
      --changeLogFile=db/changelog/db.changelog-root.yaml
      update
    depends_on:
      postgres:
        condition: service_healthy

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      JDBC_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-kdiab-treatments}
      DB_USER: kdiab_treatments
      DB_PASSWORD: ${TREATMENTS_DB_PASSWORD:-kdiab_treatments_devpassword}
      JWT_DOMAIN: http://localhost:8081/realms/kdiab-treatments
      JWKS_URL: http://keycloak:8080/realms/kdiab-treatments/protocol/openid-connect/certs
      JWT_AUDIENCE: treatment
      JWT_REALM: kdiab-treatments
      CORS_ALLOWED_ORIGINS: http://localhost:3002
      JSON_PRETTY_PRINT: "true"
    ports:
      - "8083:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_started
      liquibase:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  postgres_data:
