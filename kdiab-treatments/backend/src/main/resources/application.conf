ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ org.javafreedom.kdiab.treatments.ApplicationKt.module ]
    }
}

jwt {
    domain = "http://localhost:8081/realms/kdiab-treatments"
    domain = ${?JWT_DOMAIN}
    jwksUrl = "http://localhost:8081/realms/kdiab-treatments/protocol/openid-connect/certs"
    jwksUrl = ${?JWKS_URL}
    audience = "treatment"
    audience = ${?JWT_AUDIENCE}
    realm = "kdiab-treatments"
    realm = ${?JWT_REALM}
    secret = ${?JWT_SECRET}
}

cors {
    allowedOrigins = "http://localhost:3000"
    allowedOrigins = ${?CORS_ALLOWED_ORIGINS}
}

storage {
    driverClassName = "org.postgresql.Driver"
    jdbcUrl = "jdbc:postgresql://localhost:5432/kdiab-treatments"
    jdbcUrl = ${?JDBC_URL}
    username = "postgres"
    username = ${?DB_USER}
    password = ${?DB_PASSWORD}
    maximumPoolSize = 3
    maximumPoolSize = ${?DB_POOL_SIZE}
    isAutoCommit = false
    transactionIsolation = "TRANSACTION_REPEATABLE_READ"
}
