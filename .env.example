# Template for production or CI overrides.
# The committed .env already contains dev-safe defaults — no action needed for local development.
# For production, copy this file to .env.local and set real secrets:
#   cp .env.example .env.local

POSTGRES_PASSWORD=changeme
KEYCLOAK_ADMIN_PASSWORD=changeme

# Optional overrides (defaults shown)
# POSTGRES_USER=postgres
# KEYCLOAK_ADMIN=admin
