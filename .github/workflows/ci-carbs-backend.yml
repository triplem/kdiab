name: kdiab-carbs / Backend CI

on:
  push:
    branches: [ "**" ]
    paths:
      - 'kdiab-carbs/src/**'
      - 'kdiab-carbs/gradle/**'
      - 'kdiab-carbs/api/openapi.yaml'
      - 'kdiab-carbs/build.gradle.kts'
      - 'kdiab-carbs/settings.gradle.kts'
      - 'kdiab-carbs/gradle.properties'
      - 'kdiab-carbs/Dockerfile'
      - '.trivyignore'
      - 'kdiab-common/src/**'
      - '.github/workflows/ci-carbs-backend.yml'
      - '.github/workflows/backend-ci-reusable.yml'
      - 'build-logic/**'
      - 'gradle/libs.versions.toml'
  pull_request:
    branches: [ "**" ]
    paths:
      - 'kdiab-carbs/src/**'
      - 'kdiab-carbs/gradle/**'
      - 'kdiab-carbs/api/openapi.yaml'
      - 'kdiab-carbs/build.gradle.kts'
      - 'kdiab-carbs/settings.gradle.kts'
      - 'kdiab-carbs/gradle.properties'
      - 'kdiab-carbs/Dockerfile'
      - '.trivyignore'
      - 'kdiab-common/src/**'
      - '.github/workflows/ci-carbs-backend.yml'
      - '.github/workflows/backend-ci-reusable.yml'
      - 'build-logic/**'
      - 'gradle/libs.versions.toml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read
  security-events: write

jobs:
  build:
    uses: ./.github/workflows/backend-ci-reusable.yml
    with:
      service: kdiab-carbs
      sonar_project_key: triplem_kdiab-carbs
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
