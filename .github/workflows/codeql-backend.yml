name: CodeQL / Backend (java-kotlin)

on:
  push:
    branches: [ "main" ]
    paths:
      - '**/src/**/*.kt'
      - '**/build.gradle.kts'
      - 'build-logic/**'
      - 'gradle/libs.versions.toml'
      - '.github/workflows/codeql-backend.yml'
      - '.github/codeql/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - '**/src/**/*.kt'
      - '**/build.gradle.kts'
      - 'build-logic/**'
      - 'gradle/libs.versions.toml'
      - '.github/workflows/codeql-backend.yml'
      - '.github/codeql/**'
  schedule:
    - cron: '0 6 * * 1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  analyze:
    name: Analyze (java-kotlin)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: java-kotlin
          build-mode: manual
          queries: security-and-quality
          config-file: .github/codeql/codeql-config.yml

      - name: Build for CodeQL analysis
        # Serialize the composite build: included builds' :classes tasks otherwise
        # run in parallel and a service's compileKotlin can race ahead of its
        # openApiGenerate, causing intermittent "Cannot infer type for T" failures.
        # Compile-only, so single-worker throughput is a non-issue.
        run: ./gradlew compileBackends --no-daemon --no-build-cache --no-parallel --max-workers=1

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:java-kotlin
