name: kdiab-calc / Backend CI

on:
  push:
    branches: [ "**" ]
    paths:
      - 'kdiab-calc/src/**'
      - 'kdiab-calc/gradle/**'
      - 'kdiab-calc/api/openapi.yaml'
      - 'kdiab-calc/build.gradle.kts'
      - 'kdiab-calc/settings.gradle.kts'
      - 'kdiab-calc/gradle.properties'
      - '.github/workflows/ci-calc-backend.yml'
  pull_request:
    branches: [ "**" ]
    paths:
      - 'kdiab-calc/src/**'
      - 'kdiab-calc/gradle/**'
      - 'kdiab-calc/api/openapi.yaml'
      - 'kdiab-calc/build.gradle.kts'
      - 'kdiab-calc/settings.gradle.kts'
      - 'kdiab-calc/gradle.properties'
      - '.github/workflows/ci-calc-backend.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Determine Version
        run: |
          if [[ "${{ github.ref }}" == 'refs/heads/main' ]]; then
            echo "VERSION=latest" >> $GITHUB_ENV
          else
            BRANCH_NAME=${GITHUB_REF#refs/heads/}
            SAFE_BRANCH_NAME=$(echo "$BRANCH_NAME" | sed 's/[^a-zA-Z0-9]/-/g' | tr '[:upper:]' '[:lower:]')
            SHORT_SHA=$(git rev-parse --short HEAD)
            echo "VERSION=${SAFE_BRANCH_NAME}-${SHORT_SHA}-SNAPSHOT" >> $GITHUB_ENV
          fi

      - name: Build and Test
        run: cd kdiab-calc && ./gradlew :check :buildFatJar :cyclonedxBom -Pversion=${{ env.VERSION }}

      - name: SonarCloud Scan
        if: ${{ env.SONAR_TOKEN != '' }}
        uses: SonarSource/sonarcloud-github-action@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          projectBaseDir: kdiab-calc
          args: >
            -Dsonar.projectKey=triplem_kdiab-calc
            -Dsonar.organization=triplem
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.sources=src/main/kotlin
            -Dsonar.tests=src/test/kotlin,src/e2e-test/kotlin,src/integration-test/kotlin
            -Dsonar.java.binaries=build/classes/kotlin/main
            -Dsonar.coverage.jacoco.xmlReportPaths=build/reports/kover/report.xml

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker Image
        uses: docker/build-push-action@v7
        with:
          context: kdiab-calc
          file: kdiab-calc/Dockerfile
          push: false
          tags: |
            kdiab-calc-backend:latest
            kdiab-calc-backend:${{ env.VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=docker,dest=/tmp/backend-image.tar

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: kdiab-calc-backend:${{ env.VERSION }}
          format: 'table'
          exit-code: '1'
          severity: 'HIGH,CRITICAL'
          ignore-unfixed: true

      - name: Upload Docker Image
        uses: actions/upload-artifact@v7
        with:
          name: calc-backend-image
          path: /tmp/backend-image.tar
          retention-days: 7

      - name: Upload SBOM
        uses: actions/upload-artifact@v7
        with:
          name: calc-backend-bom
          path: kdiab-calc/build/reports/cyclonedx/bom.json
          retention-days: 7
