# =============================================================================
# kdiab — Full-stack integration compose
#
# Starts all services with a shared Keycloak realm and a single PostgreSQL
# instance containing all three service databases, pre-seeded with 30 days
# of realistic CGM + treatment + profile data for sarah and mike.
#
# Service URLs (after startup):
#   Keycloak Admin:       http://localhost:8081  (admin / admin)
#   pgAdmin:              http://localhost:5050   (opt-in: add -f docker-compose.pgadmin.yml)
#   kdiab-ui (unified):    http://localhost:3005
#   Measures   API:    http://localhost:8080
#   Profiles   API:    http://localhost:8082
#   Treatments API:    http://localhost:8083
#   Analyze    API:    http://localhost:8084
#
# Test accounts (password: password):
#   sarah      PATIENT (mg/dL)
#   mike       PATIENT (mmol/L)
#   dr_house   DOCTOR  (assigned to sarah)
#   dr_cameron DOCTOR  (assigned to mike)
#   admin      ADMIN
#
# Shared Keycloak realm: kdiab
# The BFF client token carries all four JWT audiences (analyze, measure, profile,
# treatment) so it can forward the user's JWT to every upstream service.
#
# Prerequisites: copy .env.example to .env and set real values before first run.
# =============================================================================

services:

  # ── Infrastructure ──────────────────────────────────────────────────────────

  postgres:
    container_name: kdiab-postgres
    image: postgres:15.18@sha256:eb37f58646a901dc7727cf448cae36daaefaba79de33b5058dab79aa4c04aefb
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postgres
      # Per-service role passwords — passed to 01-init-databases.sh at first startup.
      MEASURES_DB_PASSWORD: ${MEASURES_DB_PASSWORD}
      PROFILES_DB_PASSWORD: ${PROFILES_DB_PASSWORD}
      TREATMENTS_DB_PASSWORD: ${TREATMENTS_DB_PASSWORD}
      CARBS_DB_PASSWORD: ${CARBS_DB_PASSWORD}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./config/postgres/01-init-databases.sh:/docker-entrypoint-initdb.d/01-init-databases.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - shared
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '2.0'
        reservations:
          memory: 512M
          cpus: '1.0'

  keycloak:
    container_name: kdiab-keycloak
    image: quay.io/keycloak/keycloak:26.6.1@sha256:dea26401d06341095cc4ea9d66896200b55de5ca1daa1d2fcbe58493afa6e0ad
    command: start-dev --import-realm
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
    volumes:
      - ./config/keycloak-realm.json:/opt/keycloak/data/import/realm.json:ro
      - ./kdiab-measures/config/keycloak-theme:/opt/keycloak/themes/kdiab:ro
    ports:
      - "127.0.0.1:8081:8080"
    networks:
      - shared
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.5'

  # ── Schema migrations ───────────────────────────────────────────────────────

  liquibase-measures:
    container_name: kdiab-measures-liquibase
    image: localhost/kdiab-measures-liquibase:latest
    build:
      context: ./kdiab-measures
      dockerfile: Dockerfile.liquibase
    command: >
      --url=jdbc:postgresql://postgres:5432/kdiab-measures
      --username=${POSTGRES_USER:-postgres}
      --password=${POSTGRES_PASSWORD}
      --searchPath=/liquibase
      --changeLogFile=db/changelog/db.changelog-root.yaml
      update
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - shared
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

  liquibase-profiles:
    container_name: kdiab-profiles-liquibase
    image: localhost/kdiab-profiles-liquibase:latest
    build:
      context: ./kdiab-profiles
      dockerfile: Dockerfile.liquibase
    command: >
      --url=jdbc:postgresql://postgres:5432/kdiab-profiles
      --username=${POSTGRES_USER:-postgres}
      --password=${POSTGRES_PASSWORD}
      --searchPath=/liquibase
      --changeLogFile=db/changelog/db.changelog-master.yaml
      update
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - shared
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

  liquibase-treatments:
    container_name: kdiab-treatments-liquibase
    image: localhost/kdiab-treatments-liquibase:latest
    build:
      context: ./kdiab-treatments
      dockerfile: Dockerfile.liquibase
    command: >
      --url=jdbc:postgresql://postgres:5432/kdiab-treatments
      --username=${POSTGRES_USER:-postgres}
      --password=${POSTGRES_PASSWORD}
      --searchPath=/liquibase
      --changeLogFile=db/changelog/db.changelog-root.yaml
      update
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - shared
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

  liquibase-carbs:
    container_name: kdiab-carbs-liquibase
    image: localhost/kdiab-carbs-liquibase:latest
    build:
      context: ./kdiab-carbs
      dockerfile: Dockerfile.liquibase
    command: >
      --url=jdbc:postgresql://postgres:5432/kdiab-carbs
      --username=${POSTGRES_USER:-postgres}
      --password=${POSTGRES_PASSWORD}
      --searchPath=/liquibase
      --changeLogFile=db/changelog/db.changelog-root.yaml
      update
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - shared

  carbs-backend:
    container_name: kdiab-carbs-backend
    image: localhost/kdiab-carbs-backend:latest
    build:
      context: ./kdiab-carbs
      dockerfile: Dockerfile
    environment:
      JDBC_URL: jdbc:postgresql://postgres:5432/kdiab-carbs
      DB_USER: kdiab_carbs
      DB_PASSWORD: ${CARBS_DB_PASSWORD}
      JWT_DOMAIN: http://localhost:8081/realms/kdiab
      JWKS_URL: http://keycloak:8080/realms/kdiab/protocol/openid-connect/certs
      JWT_AUDIENCE: carbs
      JWT_REALM: kdiab
      CORS_ALLOWED_ORIGINS: http://localhost:3005
      OTEL_SERVICE_NAME: kdiab-carbs
      OTEL_TRACES_EXPORTER: ${OTEL_TRACES_EXPORTER:-none}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://localhost:4318}
    ports:
      - "8085:8085"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8085/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      liquibase-carbs:
        condition: service_completed_successfully
      keycloak:
        condition: service_started
    networks:
      shared:
      kdiab-ui-net:
        aliases:
          - carbs-backend
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.5'

  # Runs after all Liquibase migrations to insert seed data
  pg-seed:
    container_name: kdiab-pg-seed
    image: postgres:15.18@sha256:eb37f58646a901dc7727cf448cae36daaefaba79de33b5058dab79aa4c04aefb
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./config/postgres/02-seed-data.sql:/seed.sql:ro
    command: >
      psql -h postgres -U ${POSTGRES_USER:-postgres} -f /seed.sql
    depends_on:
      liquibase-measures:
        condition: service_completed_successfully
      liquibase-profiles:
        condition: service_completed_successfully
      liquibase-treatments:
        condition: service_completed_successfully
      liquibase-carbs:
        condition: service_completed_successfully
    networks:
      - shared
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'

  # ── kdiab-measures ──────────────────────────────────────────────────────────

  measures-backend:
    container_name: kdiab-measures-backend
    image: localhost/kdiab-measures-backend:latest
    build:
      context: ./kdiab-measures
      dockerfile: Dockerfile
    environment:
      JDBC_URL: jdbc:postgresql://postgres:5432/kdiab-measures
      DB_USER: kdiab_measures
      DB_PASSWORD: ${MEASURES_DB_PASSWORD}
      JWT_DOMAIN: http://localhost:8081/realms/kdiab
      JWKS_URL: http://keycloak:8080/realms/kdiab/protocol/openid-connect/certs
      JWT_AUDIENCE: measure
      JWT_REALM: kdiab
      CORS_ALLOWED_ORIGINS: http://localhost:3005
      OTEL_SERVICE_NAME: kdiab-measures
      OTEL_TRACES_EXPORTER: ${OTEL_TRACES_EXPORTER:-none}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://localhost:4318}
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      pg-seed:
        condition: service_completed_successfully
      keycloak:
        condition: service_started
    networks:
      shared:
      kdiab-ui-net:
        aliases:
          - measures-backend
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.5'

  profiles-backend:
    container_name: kdiab-profiles-backend
    image: localhost/kdiab-profiles-backend:latest
    build:
      context: ./kdiab-profiles
      dockerfile: Dockerfile
    environment:
      JDBC_URL: jdbc:postgresql://postgres:5432/kdiab-profiles
      DB_USER: kdiab_profiles
      DB_PASSWORD: ${PROFILES_DB_PASSWORD}
      JWT_DOMAIN: http://localhost:8081/realms/kdiab
      JWKS_URL: http://keycloak:8080/realms/kdiab/protocol/openid-connect/certs
      JWT_AUDIENCE: profile
      JWT_REALM: kdiab
      CORS_ALLOWED_ORIGINS: http://localhost:3005
      OTEL_SERVICE_NAME: kdiab-profiles
      OTEL_TRACES_EXPORTER: ${OTEL_TRACES_EXPORTER:-none}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://localhost:4318}
    ports:
      - "8082:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      pg-seed:
        condition: service_completed_successfully
      keycloak:
        condition: service_started
    networks:
      - shared
      - kdiab-ui-net
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.5'

  treatments-backend:
    container_name: kdiab-treatments-backend
    image: localhost/kdiab-treatments-backend:latest
    build:
      context: ./kdiab-treatments
      dockerfile: Dockerfile
    environment:
      JDBC_URL: jdbc:postgresql://postgres:5432/kdiab-treatments
      DB_USER: kdiab_treatments
      DB_PASSWORD: ${TREATMENTS_DB_PASSWORD}
      JWT_DOMAIN: http://localhost:8081/realms/kdiab
      JWKS_URL: http://keycloak:8080/realms/kdiab/protocol/openid-connect/certs
      JWT_AUDIENCE: treatment
      JWT_REALM: kdiab
      CORS_ALLOWED_ORIGINS: http://localhost:3005
      OTEL_SERVICE_NAME: kdiab-treatments
      OTEL_TRACES_EXPORTER: ${OTEL_TRACES_EXPORTER:-none}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://localhost:4318}
    ports:
      - "8083:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      pg-seed:
        condition: service_completed_successfully
      keycloak:
        condition: service_started
    networks:
      shared:
      kdiab-ui-net:
        aliases:
          - treatments-backend
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.5'

  calc-backend:
    container_name: kdiab-calc-backend
    image: localhost/kdiab-calc-backend:latest
    build:
      context: ./kdiab-calc
      dockerfile: Dockerfile
    environment:
      PROFILES_URL: http://profiles-backend:8080
      JWT_DOMAIN: http://localhost:8081/realms/kdiab
      JWKS_URL: http://keycloak:8080/realms/kdiab/protocol/openid-connect/certs
      JWT_AUDIENCE: calc
      JWT_REALM: kdiab
      CORS_ALLOWED_ORIGINS: http://localhost:3005
      OTEL_SERVICE_NAME: kdiab-calc
      OTEL_TRACES_EXPORTER: ${OTEL_TRACES_EXPORTER:-none}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://localhost:4318}
    ports:
      - "8086:8086"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8086/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      profiles-backend:
        condition: service_healthy
      keycloak:
        condition: service_started
    networks:
      shared:
      kdiab-ui-net:
        aliases:
          - calc-backend
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
        reservations:
          memory: 128M
          cpus: '0.25'

  analyze-backend:
    container_name: kdiab-analyze-backend
    image: localhost/kdiab-analyze-backend:latest
    build:
      context: ./kdiab-analyze
      dockerfile: Dockerfile
    environment:
      MEASURES_URL: http://measures-backend:8080
      PROFILES_URL: http://profiles-backend:8080
      TREATMENTS_URL: http://treatments-backend:8080
      JWT_DOMAIN: http://localhost:8081/realms/kdiab
      JWKS_URL: http://keycloak:8080/realms/kdiab/protocol/openid-connect/certs
      JWT_AUDIENCE: analyze
      JWT_REALM: kdiab
      CORS_ALLOWED_ORIGINS: http://localhost:3005
      OTEL_SERVICE_NAME: kdiab-analyze
      OTEL_TRACES_EXPORTER: ${OTEL_TRACES_EXPORTER:-none}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://localhost:4318}
    ports:
      - "8084:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    depends_on:
      measures-backend:
        condition: service_healthy
      profiles-backend:
        condition: service_healthy
      treatments-backend:
        condition: service_healthy
      keycloak:
        condition: service_started
    networks:
      - shared
      - kdiab-ui-net
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'

  kdiab-ui:
    container_name: kdiab-ui
    image: localhost/kdiab-ui:latest
    build:
      context: ./kdiab-ui
      dockerfile: Dockerfile
      args:
        VITE_OIDC_AUTHORITY: http://localhost:8081/realms/kdiab
        VITE_OIDC_CLIENT_ID: kdiab-ui
    ports:
      - "3005:8080"
    depends_on:
      measures-backend:
        condition: service_healthy
      profiles-backend:
        condition: service_healthy
      treatments-backend:
        condition: service_healthy
      analyze-backend:
        condition: service_healthy
    networks:
      - kdiab-ui-net
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: '0.25'
        reservations:
          memory: 32M
          cpus: '0.1'

  # ── Gateway ─────────────────────────────────────────────────────────────────
  # Unified reverse proxy: localhost:3000/{measures,profiles,treatments,analyze}/
  # Routes requests to the appropriate frontend by stripping the service path prefix.
  # Each frontend is built with VITE_BASE_PATH matching its location block so
  # asset references and API calls are prefixed correctly.

networks:
  shared:    # postgres, keycloak, all backends, liquibase, seed
  kdiab-ui-net:  # kdiab-ui ↔ all four backends

volumes:
  postgres_data:
