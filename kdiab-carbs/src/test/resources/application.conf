ktor {
    deployment {
        port = 8085
    }
    application {
        modules = [ org.javafreedom.kdiab.carbs.ApplicationKt.module ]
    }
}

jwt {
    domain = "http://localhost:8081/realms/kdiab-carbs"
    audience = "carbs"
    realm = "kdiab-carbs"
    test = true
    allowTestMode = true
    secret = "test-secret-for-unit-tests-only"
}

storage {
    driverClassName = "org.h2.Driver"
    jdbcUrl = "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL"
    username = "sa"
    password = ""
    maximumPoolSize = 3
    isAutoCommit = false
    transactionIsolation = "TRANSACTION_REPEATABLE_READ"
}
